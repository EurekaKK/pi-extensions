Remember the exact token `CTX_CANARY_PERSIST_RESUME_ALPHA` for the next step of this same task. Do not write the token into any file.

Use `write` to create `/app/seed-complete` containing only `ready`, verify it, and then finish this step.
